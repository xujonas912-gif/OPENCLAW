# 电商运营 Agent 规则文件：后台自动运行与确认执行（v1）

## 1. 作用

本文件定义后台自动化模块的任务分类、执行边界、确认机制、失败处理、日志审计，以及与巡检 / 客服 / 商品模块的联动方式。

目标不是让系统随意操作后台，而是：
- 让低风险任务稳定自动运行
- 让中风险任务先准备动作
- 让高风险任务必须确认后执行
- 让每一步有边界、有中断、有记录

---

## 2. 适用场景

在以下场景使用本规则文件：
- 定时巡检任务
- 报表采集任务
- 客服草稿准备任务
- 商品问题检查任务
- 待确认执行任务
- 后台固定 SOP 自动化

不适用于：
- 无确认的高风险后台提交
- 无边界的跨店批量操作
- 无日志的自动执行

---

## 3. 任务模式分类

后台自动化任务分为四类：

## 3.1 只读运行
定义：
- 只采集
- 只判断
- 只输出
- 不写后台

适用任务：
- 巡检
- 报表导出
- 状态检查
- 评价监控
- 消息监控
- 商品状态检查

风险级别：低

默认策略：
- 优先自动化
- 可定时运行

---

## 3.2 准备执行
定义：
- 自动分析
- 自动生成建议
- 自动填好草稿/动作计划
- 不真正提交

适用任务：
- 客服草稿生成
- 商品修改建议
- 调价建议单
- 补货建议单
- 批量动作清单

风险级别：中低

默认策略：
- 可自动化
- 输出到待确认池

---

## 3.3 确认后执行
定义：
- 系统先准备动作
- 人工确认
- 再执行提交

适用任务：
- 批量回复
- 白名单商品修改
- 固定 SOP 后台操作
- 低范围批量处理

风险级别：中高

默认策略：
- 必须确认
- 必须记录日志
- 必须有限范围

---

## 3.4 禁止自动执行
定义：
- 默认不能交给系统自主完成
- 必须人工亲自决定与操作

适用任务：
- 退款/赔偿最终处理
- 高风险改价
- 大范围上下架
- 平台争议最终回复
- 高风险公开回复
- 跨店无白名单批量动作

风险级别：高

默认策略：
- 系统只能提醒 / 准备建议
- 不得自动提交

---

## 4. 模块联动关系

### 4.1 巡检模块 → 自动化模块
- 巡检可定时触发
- 巡检结果可自动生成日报与异常清单
- P0/P1 风险可单独提醒

### 4.2 客服模块 → 自动化模块
- 标准消息可自动生成草稿
- 中风险消息进入待确认池
- 高风险消息只生成处理建议

### 4.3 商品模块 → 自动化模块
- 商品问题可自动生成检查结果
- 可输出待维护队列
- 涉及正式修改时必须进入确认流程

---

## 5. 确认机制

## 5.1 确认闸门定义
任何“确认后执行”任务，在执行前必须经过确认闸门。

确认闸门至少包含：
- 动作对象
- 动作原因
- 风险说明
- 影响范围
- 预计执行数量
- 是否确认执行

---

## 5.2 确认前必须展示的信息
对于每一项待执行动作，必须展示：
- 作用对象（订单/商品/消息）
- 建议动作内容
- 触发依据
- 风险等级
- 影响范围
- 是否可回滚（如适用）

---

## 5.3 确认后执行要求
确认后执行时必须：
- 限制执行数量
- 限定执行范围
- 每步记录日志
- 发生异常立即停止

---

## 6. 默认禁止自动的动作

以下动作默认禁止自动执行：
- 自动退款
- 自动赔偿承诺
- 自动高风险改价
- 自动大范围上下架
- 自动高风险公开回复评价
- 自动发送争议类客服消息
- 自动跨店批量操作
- 自动处理平台投诉/介入

系统最多只能：
- 提醒
- 生成建议
- 准备待确认动作

---

## 7. 默认必须确认的动作

以下动作默认必须经过人工确认：
- 对外发送消息
- 批量回复消息或评价
- 批量保存商品修改
- 提交价格变更
- 提交库存相关变更
- 提交上下架状态变更
- 活动报名/配置类提交

---

## 8. 可优先自动化的动作

以下动作适合优先自动化：
- 巡检
- 汇总
- 提醒
- 生成草稿
- 生成建议单
- 导出报表
- 导出待办列表
- 固定格式日报/周报

---

## 9. 失败即停规则

满足以下任一条件时，自动化流程必须立即停止：
- 登录状态异常
- 页面结构不符合预期
- 关键元素缺失
- 数据提取失败
- 执行对象数量异常
- 执行范围超出白名单
- 需要确认但未确认
- 返回结果与预期明显不符

停止后必须：
- 记录失败点
- 标记当前任务中断
- 输出人工检查提示

---

## 10. 数量限制规则

为防止误伤扩大，默认设置数量上限：
- 单次草稿生成：可限制在固定数量内
- 单次待确认动作：必须限制数量
- 单次批量执行：必须设置最大值
- 超过阈值时，强制要求拆批或人工复核

后续各平台可设置具体阈值。

---

## 11. 白名单规则

确认执行前，必须确认动作是否落在白名单范围内。

白名单可以包括：
- 指定店铺
- 指定商品池
- 指定消息类型
- 指定活动类型
- 指定测试环境/测试对象

默认原则：
- 无白名单，不放开执行
- 范围不清晰，不执行

---

## 12. 日志审计规则

每次自动化任务至少记录：
- 任务名称
- 运行时间
- 运行模块
- 输入来源
- 提取结果
- 生成建议
- 是否进入确认闸门
- 执行动作
- 成功/失败状态
- 失败原因

对于确认后执行任务，额外记录：
- 谁确认的
- 确认时间
- 执行对象数量
- 实际执行结果

---

## 13. 自动化流程模板

## 13.1 巡检流程
1. 定时触发
2. 打开后台关键页面/报表
3. 提取关键字段
4. 按规则判断异常
5. 输出日报
6. 对 P0/P1 风险单独提醒

## 13.2 客服草稿流程
1. 拉取新消息
2. 分类
3. 风险分级
4. 匹配模板
5. 生成草稿
6. 标记“可使用 / 需确认 / 人工接管”

## 13.3 商品检查流程
1. 读取商品数据
2. 检查标题/属性/SKU/库存/价格/状态
3. 输出问题清单
4. 输出优先级和建议动作
5. 高风险项送人工确认池

## 13.4 确认执行流程
1. 从待确认池取任务
2. 展示动作详情
3. 人工确认
4. 执行动作
5. 记录日志
6. 失败即停

---

## 14. 风险边界

后台自动化模块默认可做：
- 读取
- 汇总
- 判断
- 草稿准备
- 计划准备
- 提醒
- 确认后的小范围执行

后台自动化模块默认不做：
- 无确认的高风险动作
- 无范围限制的批量操作
- 无日志的执行
- 越权操作

---

## 15. 输出模板建议

### 15.1 待确认动作模板
- 任务：xxx
- 对象：xxx
- 建议动作：xxx
- 风险等级：xxx
- 影响范围：xxx
- 执行数量：xxx
- 是否确认：待确认

### 15.2 自动化失败模板
- 任务：xxx
- 失败环节：xxx
- 失败原因：xxx
- 当前状态：已停止
- 建议动作：人工检查

---

## 16. 后续待补充

后续迭代时补：
- 平台级执行白名单策略
- 不同后台的页面容错策略
- 登录态恢复规则
- 回滚策略（如适用）
- 多店铺任务编排规则
