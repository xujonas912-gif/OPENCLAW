## Skill Name: Image Data Extractor (图像数据提取器)
## Skill ID: image-data-extractor
## 描述: 这是一个核心的视觉数据解析管道，用于处理来自电商平台（如1688, Shopee）的原始图片素材，并将其转化为结构化的、可用于后续流程（如SOP v2.0）的结构化数据。
## 触发条件: 当用户输入包含图片，且明确要求分析“图片背后的业务信息”时。

### 🛠️ **核心流程 (The Pipeline):**
1.  **输入 (Input):** 接收多张图片 (`ImageSet`)。
2.  **步骤 1：基础识别 (OCR & Object Detection):**
    *   **OCR：** 扫描所有图片，提取所有可见的**【文字信息】**（如：成分表、型号、规格参数）。
    *   **对象检测：** 识别图片中的主要物体和组件（如：拉绳、尼龙扣、XX材质的部件）。
3.  **步骤 2：逻辑推理 (Contextual Inference):** (这是最关键的一步，需要Agent思考)
    *   **功能推理：** 根据物体和文字，推断出该组件的**“商业用途”**（例如：不是“拉绳”，而是“用于牵引和安全控制的耐用牵引绳”）。
    *   **安全/合规检查：** 检查是否有任何关于**“警告”、“限用年龄”、“材料安全等级”**的文字，并将其提升到最高优先级。
4.  **输出 (Output):**
    *   生成一个标准JSON结构，而不是简单地描述图片。

### 📝 **输出结构定义 (The Schema):**
```json
{
  "extracted_items": [
    {
      "item_name": "提取出的商品名称或部件名",
      "detected_role": "该部件在哪个环节起到什么作用 (如：核心抓握点/安全扣件)"
    }
  ],
  "extracted_text": {
    "component_spec": "从图片中提取的规格文字，例如：材质：PP/尼龙；尺寸：XX*XX*XX；",
    "warning_disclaimer": "所有法律声明或安全警告文字。"
  },
  "suggested_use_case": "基于视觉和文字，建议的商业卖点（供SOP调用）。"
}
```

**⭐ 学习说明：**
这个技能本身不是“看图”，而是**“为图片预处理数据”**的能力。它允许我们的主流程（SOP）调用它，从而达到**“从图到表”**的飞跃。
